[pytest]
# Le domaine vit dans app/ (imports absolus : domain.model, domain.services…).
# On ajoute app/ ET la racine (pour le package tests/) au sys.path.
pythonpath = app .
testpaths = tests
addopts = -q --cov=app --cov-report=term-missing
