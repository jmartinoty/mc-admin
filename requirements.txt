# Bornes de version : on borne le MAJEUR (évite les ruptures d'API) sans figer
# les patchs. Pour un lock reproductible : pip freeze > requirements.lock
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
jinja2>=3.1,<4.0
python-multipart>=0.0.9,<1.0     # parsing des formulaires POST (login, actions)
itsdangerous>=2.1,<3.0           # signature des cookies de session
argon2-cffi>=21.3,<25.0          # hachage des mots de passe
pyyaml>=6.0,<7.0                 # chargement config rôles/utilisateurs
docker>=7.0,<8.0                 # docker-py, pointé sur le socket-proxy via DOCKER_HOST
# RCON : implémentation maison en stdlib (adapters/rcon.py) — la lib mcrcon
# utilise signal.SIGALRM, incompatible avec le threadpool de FastAPI.
