target-version = "py312"
line-length = 120

[lint]
select = ["E", "F", "W"]
ignore = [
    "E402",  # import not at top — toléré pour éviter les cycles
    "E501",  # line too long — acceptable ici
]
